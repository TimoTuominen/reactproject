/*import { useState, useEffect } from "react";

const [ravintoladatahaku, setravintoladatahaku] = useState([]);

//const Ravintoladatahaku = () => {
//const [ravintoladatahaku1, setravintoladatahaku1] = useState([]);
const fetchData = async () => {
  const response = await fetch("192.168.0.100:8000/api/restaurant");
  const data = await response.json();
  console.log(data);
  setravintoladatahaku(data);
};

return (
      <div>
        {users.length > 0 && (
          <ul>
            {users.map(user => (
              <li key={user.id}>{user.name}</li>
            ))}
          </ul>
        )} 
      </div>
    )
    
};
const ravintoladatahaku = [
  {
    address: "jokukatu",
    borough: "Manhattan",
    cusine: "American",
    nameof: "Timpan ravintola",
    restaurant_id: 12345678,
  },
  {
    address: "jokukatu 2",
    borough: "Manhattan",
    cusine: "American",
    nameof: "Timpan ravintola 2",
    restaurant_id: 12345678,
  },
  {
    address: "jokukatu 3",
    borough: "Manhattan",
    cusine: "American",
    nameof: "Timpan ravintola 3",
    restaurant_id: 12345678,
  },
];

export default ravintoladatahaku; */
